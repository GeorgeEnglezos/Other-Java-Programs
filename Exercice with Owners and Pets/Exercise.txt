    /**
     * Πρόγραμμα που να διαβάζει συνέχεια στοιχεία χρηστών και τον κατοικιδίων τους.
     *
     * Κλάση Pet:
     * Έχει στοιχεία:  Όνομα, Ηλικία, Όνομα ιδιοκτήτη, είδος
     * Constructors, Getters, Setters
     * Μέθοδος showPetInfo() που εμφανίζει όλα τα στοιχεία του κατοικιδίου
     *
     * Κλάση Owner:
     * Έχει στοιχεία:  Όνομα, Ηλικία, Λίστα με κατοικίδια
     * Constructor, getters και setters.
     * Μέθοδος showUserInfo() εμφανίζει τα στοιχεία ενός χρήστη.
     * Μέθοδος addNewPet() δημιουργούμε και προσθέτουμε στην λίστα του χρήστη ένα καινούργιο κατοικίδιο.
     * Μέθοδος removePet() ρωτάει το όνομα του κατοικιδιου θέλουμε να αφαιρέσουμε από τον χρήστη και το βγάζει από την λίστα
     * Mέθοδος getUserWithMostPets( ) παίρνει την λίστα με τους ιδιοκτήτες, βρίσκει ποιος έχει τα πιο πολλά κατοικίδια και επιστρέφει το όνομα του.
     *
     * Κλάση Main:
     *     1)Μέθοδος main που καλεί την showMenu() και μετά με switch κάνει όλες τις υπόλοιπες ενέργειες
     *     2)Μέθοδος showMenu() που εμφανίζει στον χρήστη τις επιλογές που έχει και επιστρέφει στην μέθοδο που την κάλεσε την απάντηση του χρήστη.
     * Δημιουργία νέου ανθρώπου.
     * Προσθήκη νέου κατοικιδίου στον άνθρωπο που θα επιλέξουμε.
     * Αφαίρεση κατοικιδίου ενός χρήστη που επιλέξουμε
     * Εμφάνιση στοιχείων χρήστη που θα επιλέξουμε.
     * Εμφάνιση στοιχείων όλων των κατοικιδίων ενός ανθρώπου.
     * Εμφάνιση του ονόματος του χρήστη με τα περισσότερα κατοικίδια.
     * @param args
     */
